apiVersion: v1
kind: Service
metadata:
  name: apache
  namespace: apache-ns
spec:
  selector:
    app: apache
  ports:
    - name: http
      port: 80  # Port exposed by the Service
      targetPort: 80
